package forcomp
import forcomp.Anagrams._
object testanagrams {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(93); 

val g = wordOccurrences("m");System.out.println("""g  : forcomp.Anagrams.Occurrences = """ + $show(g ))}
}
